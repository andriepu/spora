const getJiraComponents = require('./_get-jira-components');
const postGroomingAdd = require('./_post-grooming--add');

module.exports = async ({ title, issueKeys }) => {
  // const components = await getJiraComponents();

  // return postGroomingAdd({
  //   components,
  //   existing,
  //   issueKeys,
  // });
};
